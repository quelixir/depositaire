"""Initialise tests."""


def test_genesis():
    """
    Ensure that at least one test runs.

    Ensure at least one test (and passes) so that pytest
    does not exit with error code 5.
    """
    pass
